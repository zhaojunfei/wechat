<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.supermarket.dao.stop.SupermarketShopDao">
    
	<sql id="supermarketShopColumns">
		a.id AS "id",
		a.type_id AS "typeId",
		a.name AS "name",
		a.company AS "company",
		a.price AS "price",
		a.value AS "value",
		a.gain AS "gain",
		a.description AS "description",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="supermarketShopJoins">
	</sql>
    
	<select id="get" resultType="SupermarketShop">
		SELECT 
			<include refid="supermarketShopColumns"/>
		FROM supermarket_shop a
		<include refid="supermarketShopJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="SupermarketShop">
		SELECT 
			<include refid="supermarketShopColumns"/>
		FROM supermarket_shop a
		<include refid="supermarketShopJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="SupermarketShop">
		SELECT 
			<include refid="supermarketShopColumns"/>
		FROM supermarket_shop a
		<include refid="supermarketShopJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO supermarket_shop(
			id,
			type_id,
			name,
			company,
			price,
			value,
			gain,
			description,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{typeId},
			#{name},
			#{company},
			#{price},
			#{value},
			#{gain},
			#{description},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE supermarket_shop SET 	
			type_id = #{typeId},
			name = #{name},
			company = #{company},
			price = #{price},
			value = #{value},
			gain = #{gain},
			description = #{description},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE supermarket_shop SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>